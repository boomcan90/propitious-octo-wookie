{
    "event": "local_dev",
    "url": "https://true.localtunnel.me/{{endpoint}}",
    "requestType": "POST",
    "json": {
        "x": "{{x}}",
        "y": "{{y}}",
        "z": "{{z}}",
        "orientation": "{{orientation}}",
        "tile": "{{tile}}",
        "timestamp": "{{timestamp}}",
        "source": "{{SPARK_CORE_ID}}"
    },
    "mydevices": true
}